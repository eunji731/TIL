# 🧩 @RestController vs @Controller

## 🔥 `@RestController`
`@RestController = @Controller + @ResponseBody` 합친 것  
- 모든 메서드에 자동으로 `@ResponseBody` 적용  
- **리턴값은 뷰 이름이 아니라 응답 데이터(JSON/문자열)**  
- 데이터를 그대로 HTTP 응답으로 보내고 싶을 때 사용  
- 주로 **API 서버** 만들 때 사용  
- JSON 응답에 최적화  

---

## 🔥 `@Controller`
- 스프링의 기본 컨트롤러  
- 원래 용도: **화면(뷰)을 반환**  
- 리턴값 `String` → 뷰 이름 (JSP/Thymeleaf 등)  
- 화면 HTML 렌더링 시 사용  
- JSON 응답 시 → `@ResponseBody` 필요  

---

# 🌐 HTTP 메서드 5대 요소 정리

---

# 🔥 1) GET — 조회(Read)

## 📌 정의
리소스를 **조회(읽기)** 하는 요청.  
서버 상태가 바뀌지 않는 **멱등성**이 있음.

## 📌 특징
- 서버 데이터 변경 없음  
- 캐싱 가능  
- 조회/검색/필터에 사용  
- 브라우저에서 직접 호출 가능  

## 📌 어노테이션
- `@GetMapping`

## 📌 비유
> "냉장고 문만 열어보는 행위 — 아무 변화 없음."

---

# 🔥 2) POST — 생성(Create) / 작업 실행(Action)

## 📌 정의
새 리소스를 **생성**하거나  
서버에서 특정 **작업을 실행**하는 요청.

## 📌 특징
- 서버 데이터 변경 발생  
- Body(JSON) 필요  
- 생성, 업로드, 백엔드 작업 실행 시 사용  

## 📌 어노테이션
- `@PostMapping`

## 📌 비유
> "냉장고에 새 음료수를 넣는 행위 — 새로운 데이터 추가."

---

# 🔥 3) PUT — 전체 수정(Replace)

## 📌 정의
리소스를 **전체 교체(전체 업데이트)** 하는 요청.

## 📌 특징
- 모든 필드를 다시 보내야 함  
- 누락된 값은 덮어씌워질 수 있음  
- 멱등성 있음 (반복 호출해도 동일 결과)  

## 📌 어노테이션
- `@PutMapping`

## 📌 비유
> "냉장고 음료 전체 세트를 새 걸로 교체."

---

# 🔥 4) PATCH — 부분 수정(Partial Update)

## 📌 정의
리소스의 **일부만 수정**하는 요청.

## 📌 특징
- 필요한 필드만 보내면 됨  
- 전체 교체가 아님  
- 옵션/상태 값은 PATCH에 적합  

## 📌 어노테이션
- `@PatchMapping`

## 📌 비유
> "냉장고에서 콜라 하나만 새 걸로 바꾸는 행위."

---

# 🔥 5) DELETE — 삭제(Delete)

## 📌 정의
리소스를 **삭제**하는 요청.

## 📌 특징
- 서버 데이터 제거  
- Body 없음  
- 멱등성 있음 (여러 번 호출해도 동일한 삭제 상태 유지)  

## 📌 어노테이션
- `@DeleteMapping`

## 📌 비유
> "냉장고에서 음료 꺼내서 버리는 행위."

---

# 🎯 최종 요약 테이블

| HTTP 메서드 | Spring 어노테이션 | 역할 | 서버 변화 | Body |
|------------|------------------|-------|-----------|-------|
| **GET** | `@GetMapping` | 조회 | ❌ 없음 | ❌ 없음 |
| **POST** | `@PostMapping` | 생성 / 작업 실행 | ✔ 변화 | ✔ 있음 |
| **PUT** | `@PutMapping` | 전체 수정 | ✔ 전체 교체 | ✔ 있음 |
| **PATCH** | `@PatchMapping` | 부분 수정 | ✔ 일부 변경 | ✔ 있음 |
| **DELETE** | `@DeleteMapping` | 삭제 | ✔ 삭제됨 | ❌ 없음 |

